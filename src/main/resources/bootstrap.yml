spring:
  application:
    name: fhb-fis-o-rollback-transfer
  cloud:
    kubernetes:
      reload:
        enabled: false
      secrets:
        # contains the secrets by name specified in src/main/fabric8/deployment.yml
        paths: /etc/secrets
